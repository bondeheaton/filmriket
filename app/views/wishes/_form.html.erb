<%= form_for(Wish.new) do |f| %>
    <div class="large-12 large-centered columns">
      <%= f.text_area :message, rows: 3, placeholder: 'Skriv ditt önskemål..' %>
      <%= f.submit "Skicka", :class => 'button tiny expand', id: 'wish-button' %>
    </div>
<% end %>
