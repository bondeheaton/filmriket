<% @admin_users.each do |user| %>
    <tr>
      <td><%= user.firstname %></td>
      <td><%= user.lastname %></td>
      <td><%= user.email %></td>
      <td style="width: 10px;"><%= user.access %></td>
      <td><%= user.phonenumber %></td>
      <td><%= user.points %></td>
      <td><%= link_to 'Visa', user, :class => "login-button" %></td>
      <td><%= link_to 'Ändra', edit_user_path(user), :class => "login-button" %></td>
      <td><%= link_to 'Förstör', user, method: :delete, data: { confirm: 'Är du säker?' }, :class => "login-button" %></td>
      <td><%= link_to "Neka", disapprove_user_path(user.id), :class => "access login-button" %></td>
    </tr>
<% end %>