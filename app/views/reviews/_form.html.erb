<%= form_for(@review) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :movie_id %><br><br>
    <%= f.hidden_field :movie_id, :value => movies_to_pick %>
    <%= f.collection_select :movie_id, movies_to_pick, :id, :title, {multiple: true, 'style' => 'height:100px, width:100px'} %></p>
  </div>
  <div class="field">
    <%= f.hidden_field :videolink, :value => "inactive" %>
  </div>
  <div class="field">
    <%= f.hidden_field :active, :value => 1 %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id, :value => current_user.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
