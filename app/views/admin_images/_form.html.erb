<%= form_for(@admin_image) do |f| %>
    <% if @admin_image.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@admin_image.errors.count, "error") %> prohibited this upload from being saved:</h2>

          <ul>
            <% @admin_image.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="actions">
      <%= f.file_field :img %>
      <%= f.submit %>
    </div>

<% end %>
