<h1>Listing presses</h1>

<table>
  <thead>
    <tr>
      <th>Text</th>
      <th>Länk</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @presses.each do |press| %>
      <tr>
        <td><%= press.text %></td>
        <td><%= press.link %></td>
        <td><%= link_to 'Visa', press %></td>
        <td><%= link_to 'Redigera', edit_press_path(press) %></td>
        <td><%= link_to 'Ta bort', press, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>