<%= form_for @competition_result, remote: true do |f| %>
    <%= f.hidden_field :user_id %>
    <%= f.hidden_field :competition_id %>
    <%= f.hidden_field :score %>
  <div class="actions">
    <%= submit_tag "Skicka", id: 'competition', class: 'button tiny success' %>
  </div>
<% end %>
